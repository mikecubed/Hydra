{
  "name": "@hydra/web-gateway",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node src/server.ts",
    "start:with-web": "npm --workspace @hydra/web run build && node src/server.ts",
    "test": "node --test --experimental-test-module-mocks 'src/**/*.test.ts'"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.11",
    "@hydra/web-contracts": "*",
    "hono": "^4.12.8",
    "ws": "^8.19.0",
    "zod": "^4.3.6"
  }
}
